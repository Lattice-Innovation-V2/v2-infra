{
  "name": "v2-pulumi-gcp",
  "version": "1.0.0",
  "description": "Lattice Innovation V2 - GCP Infrastructure as Code",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "networking:preview": "cd stacks/networking && pulumi preview -s dev",
    "networking:up": "cd stacks/networking && pulumi up -s dev",
    "data:preview": "cd stacks/data && pulumi preview -s dev",
    "data:up": "cd stacks/data && pulumi up -s dev",
    "apps:preview": "cd stacks/apps && pulumi preview -s dev",
    "apps:up": "cd stacks/apps && pulumi up -s dev"
  },
  "dependencies": {
    "@pulumi/gcp": "^9.12.0",
    "@pulumi/pulumi": "^3.222.0",
    "@pulumi/random": "^4.19.1",
    "typescript": "^5.4.2"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
